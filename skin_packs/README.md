# Skin packs

Create one folder per skin pack inside this directory.

Each skin pack folder should contain:

- `Modules.png` (required): the CTM modules atlas (16×16 tiles)
- `Background.png` (optional): a tilable background texture used under painted background cells

Example:

- `skin_packs/Default/Modules.png`
- `skin_packs/Default/Background.png`
